Developer work requires these additional tools:
    autoconf
    automake
    libtool

To build from a cvs checkout or after "make maintainer-clean":
    autoreconf --install
    ./configure
    make
