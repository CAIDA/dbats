## Process this file with automake to produce Makefile.in

dist_check_SCRIPTS   = init.sh write.sh read.sh
TESTS           = init.sh write.sh read.sh
EXTRA_DIST      = test.data expected.out
CLEANFILES      = test.out

clean-local:
	    -$(RM) -rf test.dbats
